# ONGC Internship ATS - Render Production Environment Variables
# ==================================================================

# Server Configuration
NODE_ENV=production
PORT=3001
TRUST_PROXY=true

# JWT Secret - CRITICAL: Generate a new secure secret for production
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters-long-change-this-immediately

# Authentication Backend (use 'mongo' for MongoDB-only auth, or 'sql' for MySQL auth)
AUTH_BACKEND=mongo

# CORS Configuration - CRITICAL: Add your Vercel frontend URL
CORS_ORIGIN=https://your-vercel-app.vercel.app,http://localhost:5173,http://localhost:3000

# MongoDB Configuration (Primary Database)
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb+srv://manasadhikari087_db_user:YOUR_ACTUAL_PASSWORD@cluster0.yonzitr.mongodb.net/ongc-internship?retryWrites=true&w=majority&appName=Cluster0
MONGODB_DATABASE=ongc-internship

# MySQL Configuration (Authentication Database - if using SQL auth)
# You can use a free MySQL service like PlanetScale, Aiven, or JawsDB
SQL_HOST=your-mysql-host.com
SQL_PORT=3306
SQL_DATABASE=ongc_auth
SQL_USER=your-mysql-user
SQL_PASSWORD=your-mysql-password

# Email Configuration (SMTP)
# Use Gmail with App Password or other SMTP service
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-gmail-email@gmail.com
EMAIL_PASS=your-16-character-gmail-app-password

# Security & Performance
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=500
MAX_FILE_SIZE=10485760

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=/app/logs/ongc-server.log

# File Upload Configuration
UPLOAD_PATH=/app/uploads

# Optional: SSL Configuration (Render handles SSL automatically)
SSL_KEY_PATH=
SSL_CERT_PATH=